namespace CGD.Editor
{
    // What node colour means in the Map Graph window.
    public enum MapGraphViewMode
    {
        Type,
        Intensity,
        Faction,
        RequiredOptional,
        Branches
    }
}
