namespace CGD.Loot
{
    public enum LootDropKind
    {
        Stack,    // Definition × Count
        Instance, // one rolled gear piece (weapons included)
        Prefab,   // a world object to spawn
    }
}
