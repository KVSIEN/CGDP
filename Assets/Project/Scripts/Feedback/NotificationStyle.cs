namespace CGD.Feedback
{
    // How a message looks in the notification feed.
    public enum NotificationStyle
    {
        Info,
        Success,
        Reward,
        Warning,
        Danger
    }
}
